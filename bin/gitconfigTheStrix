#!/bin/bash

USERNAME=TheStrix
EMAIL=parthbhatia98@gmail.com

echo "Setting git configs for name $USERNAME with gmail email: $EMAIL"
git config --global user.name "$USERNAME"
git config --global user.email "$EMAIL"
echo "Done!"
